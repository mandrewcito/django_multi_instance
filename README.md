# django_multi_instance
django multi instance app, using defaults django users, on user register create a polls app database for him

#ToDo
* create database,folder on user register
* database routing

